import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "StockOptima | Sistema de Optimización de Inventarios",
  description: "Motor y sistema de optimización de inventarios diseñado para funcionar de manera universal en cualquier tipo de comercio",
  keywords: ["inventario", "optimización", "comercio", "stock", "gestión"],
  authors: [{ name: "StockOptima Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="es"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-gray-50 dark:bg-gray-900">
        {children}
      </body>
    </html>
  );
}
